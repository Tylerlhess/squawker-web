<!-- Trigger/Open The Modal -->
<table>
  <tr>
    <td>

    <button type="button" class="btn btn-primary text-black p-1" id="kawBtn" style="padding: 20px">Kaw Message</button>
    </td>
  </tr>
  <tr>
    <td>

    <button type="button" class="btn btn-primary text-black p-1" id="articleBtn" style="padding: 20px">Post Article</button>
    </td>
  </tr>
<tr>
    <td>

    <button type="button" class="btn btn-primary text-black p-1" id="proBtn" style="padding: 20px" width="100%">Update Profile</button>
    </td>
  </tr>
  <tr>
    <td>
      <a href="/logout" class="btn btn-primary text-black p-1" width="100%">Logout</a>
    </td>
  </tr>

</table>

<div class="modal" id="kawModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content bg-primary shadow p-1">
      <div class="modal-header">
        <h5 class="modal-title">Kaw</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

    {% include "kawModal.html.jinja" %}

    </div>
  </div>
</div>

<div class="modal" id="articleModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content bg-primary shadow p-1">
      <div class="modal-header">
        <h5 class="modal-title">Submit article</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

    {% include "articleModal.html.jinja" %}

    </div>
  </div>
</div>

<div class="modal" id="profileModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content bg-primary shadow p-1">
      <div class="modal-header">
        <h5 class="modal-title">Update Profile</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

    {% include "profileModal.html.jinja" %}

    </div>
  </div>
</div>

<div class="modal" id=cnsModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content bg-primary shadow p-1">
      <div class="modal-header">
        <h5 class="modal-title">DNS</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

    {% include "cnsModal.html.jinja" %}

    </div>
  </div>
</div>

<script>
// Get the modal
var modal = document.getElementById("kawModal");

// Get the button that opens the modal
var btn = document.getElementById("kawBtn");

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("btn-close")[0];

// When the user clicks on the button, open the modal
btn.onclick = function() {
  modal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
span.onclick = function() {
  modal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
}

// Get the modal
var artModal = document.getElementById("articleModal");

// Get the button that opens the modal
var artBtn = document.getElementById("articleBtn");

// Get the <span> element that closes the modal
var artSpan = document.getElementsByClassName("btn-close")[1];

// When the user clicks on the button, open the modal
artBtn.onclick = function() {
  artModal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
artSpan.onclick = function() {
  artModal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == artModal) {
    artModal.style.display = "none";
  }
}


// Get the modal
var proModal = document.getElementById("profileModal");

// Get the button that opens the modal
var proBtn = document.getElementById("proBtn");

// Get the <span> element that closes the modal
var proSpan = document.getElementsByClassName("btn-close")[2];

// When the user clicks on the button, open the modal
proBtn.onclick = function() {
  proModal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
proSpan.onclick = function() {
  proModal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == proModal) {
    proModal.style.display = "none";
  }
}


// Get the modal
var cnsModal = document.getElementById("cnsModal");

// Get the button that opens the modal
var cnsBtn = document.getElementById("cnsBtn");

// Get the <span> element that closes the modal
var cnsSpan = document.getElementsByClassName("btn-close")[2];

// When the user clicks on the button, open the modal
cnsBtn.onclick = function() {
  cnsModal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
cnsSpan.onclick = function() {
  cnsModal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == cnsModal) {
    cnsModal.style.display = "none";
  }
}

</script>